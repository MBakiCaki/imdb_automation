# imdb_automation
To run:

```npx playwright test```

By default:
- Uses chromium browser
- Runs parallel
- In headless mode

configurations can be edited in playwright.config.js

To run headed:

```npx playwright test --headed```
